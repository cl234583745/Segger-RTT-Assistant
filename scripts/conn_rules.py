c = c.replace('f"错误详情:\\n{error_msg}"', 'i18n("error.probe_detect_detail").format(error_msg)')
c = c.replace('"正在更新..."', 'i18n("error.updating")')
c = c.replace('"下载 CMSIS Pack"', 'i18n("dialog.download_cmsis_pack")')
c = c.replace('"输入芯片型号（如 STM32U575、R7FA6M5AF）："', 'i18n("dialog.input_chip_model")')
c = c.replace('"下载 Pack"', 'i18n("dialog.download_pack")')
c = c.replace('f"正在查找 {target} 的 Pack..."', 'f"{i18n(\'label.finding_pack\').format(target)}"')
c = c.replace('f"也可手动下载 .pack 文件复制到:\\n{_pack_dir}"', 'f"{i18n(\'label.manual_download_hint\')}\\n{_pack_dir}"')
c = c.replace('"下载成功"', 'i18n("dialog.download_success")')
c = c.replace('f"{target} 的 Pack 已安装并刷新目标列表。"', 'i18n("error.pack_installed").format(target)')
c = c.replace('"下载失败"', 'i18n("dialog.download_failed")')
c = c.replace('"请手动下载CMSIS Pack并复制到以下目录："', 'i18n("label.manually_download_pack")')
c = c.replace('QPushButton("确定")', 'QPushButton(i18n("btn.ok"))')
c = c.replace('"从J-Link DLL更新设备列表到devices.txt"', 'i18n("tooltip.update_device_list")')
c = c.replace('"under_reset: 复位状态下连接(推荐)\\n"\n            "halt_on_connect: 连接后立即暂停\\n"\n            "pre_reset: 连接前复位\\n"\n            "default: 默认模式"', 'i18n("conn.under_reset") + "\\n" + "\\n" + i18n("conn.halt_on_connect") + "\\n" + "\\n" + i18n("conn.pre_reset") + "\\n" + "\\n" + i18n("conn.default_mode")')
c = c.replace('QLabel("厂商:")', 'QLabel(i18n("label.vendor"))')
c = c.replace('QLabel("来源:")', 'QLabel(i18n("label.source"))')
c = c.replace('f"已更新设备列表: {len(device_names)}个设备\\n保存至: {info}"', 'i18n("error.updated_device_list").format(len(device_names), info)')
c = c.replace('f"设备 \'{device_name}\' 的信息不存在"', 'i18n("error.device_info_not_exist").format(device_name)')
c = c.replace('f"设备 \'{device_name}\' 无有效的RAM地址信息"', 'i18n("error.device_no_ram_addr").format(device_name)')
c = c.replace('f"设备 \'{device_name}\' 无有效的RAM大小信息"', 'i18n("error.device_no_ram_size").format(device_name)')
c = c.replace('f"填充失败: {e}"', 'f"{i18n(\'error.fill_failed\')}: {e}"')
